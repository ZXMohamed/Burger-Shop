export const name = {
    max: 50,
    min: 2
};
export const message = {
    min: 10
};
export const pinCode = {
    pattern: /^[0-9]+$/,
    min: 5
};
export const phoneNumber = {
    pattern: /^[0-9]+$/,
    min: 10
};